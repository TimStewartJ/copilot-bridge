diff --git a/node_modules/@github/copilot-sdk/dist/cjs/client.js b/node_modules/@github/copilot-sdk/dist/cjs/client.js
index 7378159..d45391f 100644
--- a/node_modules/@github/copilot-sdk/dist/cjs/client.js
+++ b/node_modules/@github/copilot-sdk/dist/cjs/client.js
@@ -275,6 +275,7 @@ class CopilotClient {
   options;
   isExternalServer = false;
   forceStopping = false;
+  stopping = false;
   /** Token sent in `connect`; auto-generated when the SDK spawns its own CLI in TCP mode. */
   effectiveConnectionToken;
   onListModels;
@@ -615,6 +616,7 @@ class CopilotClient {
    * ```
    */
   async stop() {
+    this.stopping = true;
     const errors = [];
     const activeSessions = [...this.sessions.values()];
     if (this.connectionConfig.kind === "inprocess") {
@@ -1127,6 +1129,8 @@ class CopilotClient {
         includeSubAgentStreamingEvents: config.includeSubAgentStreamingEvents ?? true,
         ...this.onGitHubTelemetry != null ? { enableGitHubTelemetryForwarding: true } : {},
         mcpServers: toWireMcpServers(config.mcpServers),
+        githubMcpToolOptions: config.githubMcpToolOptions,
+        askUserVariant: config.askUserVariant,
         mcpOAuthTokenStorage: config.mcpOAuthTokenStorage,
         envValueMode: "direct",
         customAgents: toWireCustomAgents(config.customAgents),
@@ -1348,6 +1352,8 @@ class CopilotClient {
         includeSubAgentStreamingEvents: config.includeSubAgentStreamingEvents ?? true,
         ...this.onGitHubTelemetry != null ? { enableGitHubTelemetryForwarding: true } : {},
         mcpServers: toWireMcpServers(config.mcpServers),
+        githubMcpToolOptions: config.githubMcpToolOptions,
+        askUserVariant: config.askUserVariant,
         mcpOAuthTokenStorage: config.mcpOAuthTokenStorage,
         envValueMode: "direct",
         customAgents: toWireCustomAgents(config.customAgents),
@@ -2019,6 +2025,7 @@ stderr: ${stderrOutput}`
       this.state = "error";
       const reason = err instanceof Error ? err.stack ?? err.message : String(err);
       this.logDebug(`stdin pipe error: ${reason}`);
+      console.warn(`[copilot-sdk] stdin pipe error: ${reason}`);
       try {
         this.connection?.dispose();
       } catch {
@@ -2117,9 +2124,11 @@ stderr: ${stderrOutput}`
       }
     );
     this.connection.onClose(() => {
+      if (!this.stopping && !this.forceStopping) console.warn(`[copilot-sdk] JSON-RPC connection closed unexpectedly (state was ${this.state})`);
       this.state = "disconnected";
     });
-    this.connection.onError((_error) => {
+    this.connection.onError((error) => {
+      if (!this.stopping && !this.forceStopping) console.warn(`[copilot-sdk] JSON-RPC connection error: ${error instanceof Error ? error.message : String(error)}`);
       this.state = "disconnected";
     });
   }
diff --git a/node_modules/@github/copilot-sdk/dist/client.js b/node_modules/@github/copilot-sdk/dist/client.js
index 88a2390..7ac3842 100644
--- a/node_modules/@github/copilot-sdk/dist/client.js
+++ b/node_modules/@github/copilot-sdk/dist/client.js
@@ -252,6 +252,7 @@ class CopilotClient {
   options;
   isExternalServer = false;
   forceStopping = false;
+  stopping = false;
   /** Token sent in `connect`; auto-generated when the SDK spawns its own CLI in TCP mode. */
   effectiveConnectionToken;
   onListModels;
@@ -592,6 +593,7 @@ class CopilotClient {
    * ```
    */
   async stop() {
+    this.stopping = true;
     const errors = [];
     const activeSessions = [...this.sessions.values()];
     if (this.connectionConfig.kind === "inprocess") {
@@ -1104,6 +1106,8 @@ class CopilotClient {
         includeSubAgentStreamingEvents: config.includeSubAgentStreamingEvents ?? true,
         ...this.onGitHubTelemetry != null ? { enableGitHubTelemetryForwarding: true } : {},
         mcpServers: toWireMcpServers(config.mcpServers),
+        githubMcpToolOptions: config.githubMcpToolOptions,
+        askUserVariant: config.askUserVariant,
         mcpOAuthTokenStorage: config.mcpOAuthTokenStorage,
         envValueMode: "direct",
         customAgents: toWireCustomAgents(config.customAgents),
@@ -1325,6 +1329,8 @@ class CopilotClient {
         includeSubAgentStreamingEvents: config.includeSubAgentStreamingEvents ?? true,
         ...this.onGitHubTelemetry != null ? { enableGitHubTelemetryForwarding: true } : {},
         mcpServers: toWireMcpServers(config.mcpServers),
+        githubMcpToolOptions: config.githubMcpToolOptions,
+        askUserVariant: config.askUserVariant,
         mcpOAuthTokenStorage: config.mcpOAuthTokenStorage,
         envValueMode: "direct",
         customAgents: toWireCustomAgents(config.customAgents),
@@ -1996,6 +2002,7 @@ stderr: ${stderrOutput}`
       this.state = "error";
       const reason = err instanceof Error ? err.stack ?? err.message : String(err);
       this.logDebug(`stdin pipe error: ${reason}`);
+      console.warn(`[copilot-sdk] stdin pipe error: ${reason}`);
       try {
         this.connection?.dispose();
       } catch {
@@ -2094,9 +2101,11 @@ stderr: ${stderrOutput}`
       }
     );
     this.connection.onClose(() => {
+      if (!this.stopping && !this.forceStopping) console.warn(`[copilot-sdk] JSON-RPC connection closed unexpectedly (state was ${this.state})`);
       this.state = "disconnected";
     });
-    this.connection.onError((_error) => {
+    this.connection.onError((error) => {
+      if (!this.stopping && !this.forceStopping) console.warn(`[copilot-sdk] JSON-RPC connection error: ${error instanceof Error ? error.message : String(error)}`);
       this.state = "disconnected";
     });
   }
